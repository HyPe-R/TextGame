/* Required for most / all custom headers */
#include "includes.h"
#include "string.h"
#include "boolean.h"

/* Input buffer size (including null) */
#define STDIO_BUFFER_SIZE 257

char strbuf[STDIO_BUFFER_SIZE];
string_t buftok, o;
boolean running = true;

void main() {
	while(running) {}
}

